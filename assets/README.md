# Assets Folder

This folder is intentionally present as the swap-in location for future external assets.

The current playable slice does not require bundled binary assets. Everything visual and audible ships procedurally in code so the project always boots from a clean checkout.

Reserved subfolders:

- `assets/models`
- `assets/textures`
- `assets/audio`
